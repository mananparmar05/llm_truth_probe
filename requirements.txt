# ── Core ML / Deep Learning ──────────────────────────────────────────
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0         # 4-bit / 8-bit quantization (optional, speeds up loading)

# ── Data & Benchmarks ────────────────────────────────────────────────
datasets>=2.18.0
huggingface_hub>=0.22.0
pandas>=2.0.0
numpy>=1.26.0

# ── Probe Classifiers ────────────────────────────────────────────────
scikit-learn>=1.4.0
joblib>=1.3.0

# ── Visualization ────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0
kaleido>=0.2.1               # plotly static image export

# ── Experiment Tracking ──────────────────────────────────────────────
wandb>=0.16.0
mlflow>=2.11.0               # alternative to wandb

# ── Serialisation & Storage ──────────────────────────────────────────
h5py>=3.10.0                 # HDF5 for hidden-state tensors
safetensors>=0.4.2

# ── API Serving (Phase 7) ────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ── Dev / Notebook ───────────────────────────────────────────────────
jupyterlab>=4.1.0
ipywidgets>=8.1.0
tqdm>=4.66.0
black>=24.3.0
flake8>=7.0.0
pytest>=8.1.0
pytest-cov>=5.0.0
python-dotenv>=1.0.0
